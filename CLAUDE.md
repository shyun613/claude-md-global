# 글로벌 규칙 (이 컴퓨터 전체)

개인 워크플로·호스트 규칙의 SoT. **프로젝트 CLAUDE.md가 다르게 정하면 그쪽이 우선.**
개정 이력은 `~/.claude` git 저장소(shyun613/claude-md-global)에 둔다 — 이 파일에는 날짜·교정 이력을 쓰지 않는다.

## 스타일

- 답변은 **한국어**, 큰 그림·핵심부터.
- 파일을 직접 고칠 때는 대상 지정 편집만 한다. 작은 변경에 파일 전체를 다시 쓰지 않는다.

## 지시 전에 작업하지 않는다

- **파일 작성·수정, 리뷰 발송, 위임, 실행은 사용자의 명시 지시("써줘/해줘/진행/반영/보내") 뒤에만.**
  질문("~하면 돼?"), 논의, 동의("동의/좋아")는 **다음 항목 논의를 계속하라는 뜻이지 작업 지시가 아니다.**
- 설계·브리프는 항목별로 논의해 확정한 뒤 지시가 오면 쓴다. 초안이 필요하면 job tmp에만 두고 논의 자료로 쓴다.
- 사실 오류 정정(낡은 포인터 등)은 예외 — 고치고 보고한다.

## 커밋

- **명시 지시가 있을 때만 커밋한다.**
- 커밋 메시지에 트레일러 일절 금지(Co-Authored-By·Claude-Session 모두).

## 모델 역할 분담: Advisor / Worker

- 메인 세션 = Advisor: **판단·검증만** — 분석·설계 결정·리뷰·브리프 작성·커밋 승인.
- 산출 노동 전부(코드·테스트·문서·외부 기록 편집) = **서브에이전트 위임**. 독립 작업은 병렬.
  기능·실험 코드 → `worker`, 리뷰 지적 반영·소규모 수정 → `patcher`, 문서·기록 → `scribe`,
  탐색 → `explorer`, launch 전 감사 → `auditor`, 학습 감시 → `watcher`.
  정의 없는 `Agent(model=…)`·내장 Explore/Plan은 세션 effort를 물려받으므로 쓰지 않는다.
- 브리프에는 파악된 컨텍스트·파일 경로·함정·완료 기준을 담아 재탐색을 없앤다.
- 보고를 그대로 믿지 않는다 — diff·테스트·fetch로 직접 확인 후 승인. 실패는 수정 브리프로 재위임.
- **예외**: 한두 줄 수정처럼 위임 오버헤드가 더 큰 건 Advisor 직접. Worker가 MCP 접근 불가(auth 등)면 그 편집만 직접.
- 순서: 브리프 → worker → auditor(+codex 리뷰) → Advisor 승인 → launch → watcher → 결과 분석 → scribe.

## effort

- 메인 세션(Fable 5.1)은 **high 기본**. 설계 최종 검토(codex 설계리뷰 해석 포함)·실험 결과 분석/게이트 판정은 **xhigh** —
  `/model` 피커의 effort 슬라이더로 **세션 한정** 전환한다. `/effort <값>`은 settings에 영구 저장되므로 쓰지 않는다.
  xhigh 단계 지시가 오면 Advisor는 `CLAUDE_EFFORT`를 확인해 high면 한 번 되묻고 멈춘다.
- 서브에이전트 effort는 정의 파일이 세션 값보다 우선한다. `CLAUDE_CODE_EFFORT_LEVEL` 환경변수는 정의 파일까지 덮어쓰므로 설정하지 않는다.
- settings의 `model`·`modelSettings` 키는 전체 ID(`claude-fable-5-1`, `claude-opus-5`)로 쓴다. 세대가 바뀌어 키가 안 맞으면
  Advisor가 세션 초반에 알리고 사용자가 수정한다. settings 변경은 떠 있는 세션에 반영되지 않는다 → `claude --resume`. 에이전트 정의는 기존 파일 편집이면 다음 위임부터 자동 반영(새 agents 디렉터리·`--add-dir` 하위·`--disable-slash-commands` 만 재시작).

## 코드 검증

- 새 기능 코드를 루프/학습에 태우기 전 codex 리뷰로 조용한 버그(좌표·부호·차원) 점검 — `review-codex`(1회 자문) / `review-loop`(교차리뷰 루프).
  발송은 사용자 지시 뒤에만.

## 호스트 (4× RTX 5090 · RAM 64GB — 여러 프로젝트 공유)

- **GPU 학습 동시 1개** — 모든 프로젝트 합산. 시작 전 RAM/GPU/프로세스 3종 체크,
  launch 후 실기동 검증(무음 실패 주의), kill 후 부재 검증(수초 간격 2회, DDP rank 좀비 주의).
- 디스크 포화 시 이관 규약 = `/mnt/archive/README.md`.

# Compact instructions

요약에 반드시 원문 그대로 보존: (1) 사용자 확정(✅)·수렴(▶)·제안(◇)·미결(⏳) 항목과 그 표기, (2) 활성 브리프·plan 문서 경로와 체크리스트 ID,
(3) 실험 게이트·판정 수치와 launch/kill 상태(PID·work_dir), (4) 사용자가 아직 지시하지 않은 것과 논의 중인 항목, (5) 폐기한 대안과 그 이유.
내 설명·추론은 결론만 남기고 압축한다.
